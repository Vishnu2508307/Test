--
-- Changing the searchable to include field name in clustering key and add a field type field
-- instead of just a single searchable field of list type per plugin
--
DROP TABLE plugin.searchable_by_plugin_version;