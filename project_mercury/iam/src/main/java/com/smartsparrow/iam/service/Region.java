package com.smartsparrow.iam.service;

/**
 * Represent the various data regions supported.
 *
 */
public enum Region {

    // a global data region; data replicated to all data regions.
    GLOBAL,

    // an AU data region (Sydney)
    AU,

    // an US data region (Oregon)
    US

}
