package com.smartsparrow.iam.service;

/**
 * Describes the different authentication types supported in the application
 */
public enum AuthenticationType {
    BRONTE,
    MYCLOUD,
    IES,
    OIDC,
    LTI
}
