package com.smartsparrow.rtm.message.recv;

import com.smartsparrow.rtm.message.ReceivedMessage;

/**
 * An concrete class to map an empty received message, which contains the type and (optional) id.
 */
public class EmptyReceivedMessage extends ReceivedMessage {
}
