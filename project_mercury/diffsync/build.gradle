
dependencies {
    compile project(':common')
    compile project(':dse')

    implementation group: 'org.bitbucket.cowwoc', name: 'diff-match-patch', version: '1.2'
}

test {
    useJUnitPlatform()
}
