package com.smartsparrow.courseware.data;

/**
 * Define the labels for the scenario correctness. The values are intentionally using a lower case format to avoid
 * creating extra work for the overloaded front end team. They have previously defined those values in the front end
 * code as lowercase
 */
public enum ScenarioCorrectness {

    correct,
    incorrect,
    none
}
